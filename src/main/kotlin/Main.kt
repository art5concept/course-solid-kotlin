fun main() {
    var isActive = true
    if (isActive == true) {
    // sout para acortar el printline
        println("the conditions is true")
    } else {
        println("the conditions is false")
    }

    isActive = false
    if (isActive == true) {
    //  sout para acortar el printline
        println("the conditions is true")
    } else {
        println("the conditions is false")
    }
}